# WisdomLink2 · 测试用例文档

| 文档头 | |
|---|---|
| 项目 / 文档 | wl2 / 测试用例文档 v1.0 |
| 文档 ID / 状态 | WL2-DOC-005 / 草稿（随 C-04 执行回填结果） |
| 日期 / 作者 | 2026-09-12 / HUAWEI |
| 依据 | 需求规格说明书（BR oracle §4.5）、需求分析设计文档（降级矩阵 §10.3） |

## 1. 测试策略（分层）

| 层 | 载体 | 环境 | 执行方式 |
|---|---|---|---|
| L1 规则单测 | `tests/test_rules.py`（TC-701 组前身，保留 8 例） | 纯函数 | pytest |
| L2 集成 e2e（可重复） | `tests/test_integration.py`（TC-701~720 共 20 例） | mock 链路：SQLite+FAISS+mock 模型，独立数据 | pytest，无网络依赖 |
| L3 真实厂商冒烟 | `tests/live_smoke.py`（5 步） | 真实 .env：DeepSeek+智谱+本地 ONNX 重排，独立数据 | 独立脚本（避免 pytest 环境互染） |
| L4 非功能口径 | NFR-207/222/241 抽测（并入 L2/L3） | 同上 | 同上 |

数据隔离：L2 用 `data/itest.db`+`itest_faiss`，L3 用 `data/live.db`+`live_faiss`，均不污染真实实例。

## 2. 用例清单（TC-ID）

### L2 集成（TC-701…720，全部自动化断言）

| TC | 名称 | 覆盖 | 关键断言 |
|---|---|---|---|
| TC-701 | 管理面未登录 401 | FR-111/NFR-219 | 无 token 建库 → 401 |
| TC-702 | 建库自动建根目录 | FR-102 | 根文件夹“全部文档”存在 |
| TC-703 | 文件夹创建 | FR-102 | 列表含新建项 |
| TC-704 | 上传→入库→发布 | FR-103/105/113、BR-008 | 状态机走至 published |
| TC-705 | 同内容重复上传拒绝 | FR-126、BR-002 | 409 |
| TC-706 | 父子分片+清洗哈希 | FR-105、BR-003/010 | parent/child 齐全、逐片有 hash |
| TC-707 | 清洗留痕（水印命中） | FR-115、BR-009 | clean_logs 含“机密”处理记录 |
| TC-708 | 溯源指纹：正常过/篡改告警 | FR-118、BR-010 | verify=true；篡改后 verify=false |
| TC-709 | QA 精确命中 | FR-131、BR-011 | 归一化等价问题 → qa_hit |
| TC-710 | QA 变体命中 | BR-011 | 变体问题 → qa_hit |
| TC-711 | 冷门问题不误命中 | BR-011 反例 | 无 qa_hit |
| TC-712 | RAG 问答带引用 | FR-107/118、BR-004 | citations 非空且含 chunk/snippet |
| TC-713 | QA 命中节省量 | FR-131/132、BR-012 | done.tokens_saved ≥1200 且 type=qa |
| TC-714 | 用量纯口径含厂商 | FR-132、BR-012（2026-09-12 裁决） | 无 cost/currency 字段；breakdown 含 vendor |
| TC-715 | 审计覆盖 | FR-112、NFR-218 | chat.ask 与 qa.create 在案 |
| TC-716 | 检索评测返回命中 | FR-124 | eval/run 命中已发布片段 |
| TC-717 | 图谱路未命中安全跳过 | FR-117 | link 返回空实体不报错 |
| TC-718 | 按原文重建索引 | FR-126、BR-009 | 重建后仍 published |
| TC-719 | 健康/就绪 | NFR-241 | healthz=up；readyz 组件级 |
| TC-720 | 删除进回收站 | FR-104、BR-008 | status=deleted |

### L3 真实冒烟（5 步）

真实入库（智谱 2048 维向量化）→ 真实混合检索+本地 ONNX 重排 → 真实 RAG 问答（DeepSeek 流式+引用）→ 用量统计（厂商=DeepSeek/智谱）→ 审计。

### L4 非功能与降级（并入执行）

降级矩阵抽测：Redis 不可用（内存兜底，TC-719 readyz=degraded(memory)）；LLM 401（L3 DEGRADED 行，引用照常返回）；Rerank 失败跳过（代码路径，NFR-222）。性能口径（NFR-201/202/207）留待部署后压测（B-07 阶段），本阶段仅验证功能正确性。

## 3. 覆盖矩阵（FR → TC）

| FR | TC | FR | TC |
|---|---|---|---|
| FR-102 | 702/703 | FR-115 | 707 |
| FR-103/105/113 | 704/706 | FR-117 | 717 |
| FR-104 | 720 | FR-118 | 708 |
| FR-107/118 | 712 | FR-121/124 | 716 |
| FR-111/NFR-219 | 701 | FR-126 | 705/718 |
| FR-112 | 715 | FR-131 | 709/710/711/713 |
| FR-132 | 714 | FR-109/110 | L3 配置探针（真实厂商） |
| NFR-222/241 | 719 + L3 DEGRADED 行 | | |

未覆盖（显式披露）：Wiki（FR-120，页面手工走查）、多格式解析全矩阵（FR-103 仅 md 路径，pdf/docx 靠解析器单测覆盖待补）、性能压测（NFR-201…207）、B-10 数据库层迁移测试。

## 4. 执行记录

执行结果：L1 8/8、L2 20/20、L3 4 PASS + 1 DEGRADED（DeepSeek Key 401，用户侧待办）。
